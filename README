Forth base converter
====================

Number base converter that converts decimal, binary, hex, and octal values.
* I/O for different bases
* shows stack before/after
* support for signed values
